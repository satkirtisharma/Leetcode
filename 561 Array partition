class Solution {
    public int arrayPairSum(int[] nums) {
        int maxsum=0;
        Arrays.sort(nums);
        for(int i=0;i<nums.length;i=i+2){
                maxsum+=nums[i];
        }
        return maxsum;
        
    }
}
